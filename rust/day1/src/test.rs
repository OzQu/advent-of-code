#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn example_data() {
        println!(OCEAN_DEPTHS);
        assert_eq!(OCEAN_DEPTHS, OCEAN_DEPTHS);
    }
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}